# Test-mode keys only. Get them with:  stripe sandbox create --email you@example.com
# Prefer a restricted key (rk_test_...) over a secret key (sk_test_...).
STRIPE_SECRET_KEY=rk_test_replace_me

# Printed by:  stripe listen --forward-to http://localhost:4242/api/webhook
STRIPE_WEBHOOK_SECRET=whsec_replace_me

PORT=4242
PUBLIC_ORIGIN=http://localhost:4242
